on: [push]

jobs:
  test-build:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      # Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Build the test project
        id: build
        uses: ./
        with:
          source: test_projects/hello
      # - name: Verify output
      #   run: | 
      #     ls ${GITHUB_WORKSPACE}/test_projects/hello/output
